
# Database Configuration
# Note: These settings are currently used for documentation purposes only.
# The application is using mock data in the browser environment.
# In a real production environment with a backend server, these would connect to MySQL.
VITE_DB_HOST=localhost
VITE_DB_USER=root
VITE_DB_PASSWORD=
VITE_DB_NAME=manufacturing
